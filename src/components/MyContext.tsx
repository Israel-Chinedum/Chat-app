import { createContext } from "react";

export const socketContext = createContext<any>(null);

export const LoadAnimationContext = createContext<any>(null);

export const CurrentChat = createContext<any>(null);

export const User = createContext<any>(null);

export const CurrentSpace = createContext<any>(null);

export const ChatType = createContext<any>(null);
